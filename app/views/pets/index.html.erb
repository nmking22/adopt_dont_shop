<h1>All Pets</h1>

<% @pets.each do |pet| %>
  <h2><%= pet.name %></h2>
  <%= link_to 'Update Pet', "/pets/#{pet.id}/edit" %>
  <p><%= "#{pet.image}" %></p>
  <p><%= "Age: #{pet.age}" %></p>
  <p><%= "Sex: #{pet.sex}" %></p>
  <p><%= "Location: #{pet.location}" %></p>
<% end %>
